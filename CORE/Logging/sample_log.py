#!/usr/bin/python
import logging
logging.debug('So should this')
logging.info('And this, too')
logging.warning('Watch out!')  # will print a message to the console (this is default level)
logging.critical('I told you so')  #
logging.error('I told you so')  #
